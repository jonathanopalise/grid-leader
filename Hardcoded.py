import trackgeometry
import Geometry
#from xml.dom import minidom

def controlPoints():

	cp=[]

	#dom = minidom.parse('fuji.xml')
	#print('** loading track from xml...')

	#for node in dom.getElementsByTagName('point'):
	#	print('adding point with x='+node.getAttribute('x')+' and y='+node.getAttribute('y'))
	#	cp.append(Geometry.Point(float(node.getAttribute('x'))*16,float(node.getAttribute('y'))*16))

	return cp


